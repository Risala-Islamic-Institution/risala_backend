services:
  docs:
    image: risala_backend_local_docs
    container_name: risala_backend_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./risala_backend:/app/risala_backend:z
    ports:
      - '9000:9000'
    command: /start-docs
